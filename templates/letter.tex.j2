\#{ Generated by render.py -- edit the letter YAML or templates/, never build/. }
\documentclass{resume}

\begin{document}

\#{ Same header as the resume, from the same profile.yaml, so the two match. }
\resumeheader{\VAR{ profile.name }}{%
\VAR{ profile.location }\ \textperiodcentered\ \href{mailto:\VAR{ profile.email | raw }}{\VAR{ profile.email }}%
\BLOCK{ for line in profile.link_lines }
\\%
\BLOCK{ for link in line }
\BLOCK{ if not loop.first }\ \textperiodcentered\ \BLOCK{ endif }\href{\VAR{ link.url | raw }}{\VAR{ link.label }}%
\BLOCK{ endfor }
\BLOCK{ endfor }
}

\BLOCK{ if letter.date }
{\small \VAR{ letter.date }}
\par\vspace{\lettersep}
\BLOCK{ endif }

\#{ Carries company and role, so neither can go stale in the prose unnoticed. }
{\bfseries Re: \VAR{ letter.role }\ \textperiodcentered\ \VAR{ letter.company }}
\par\vspace{\lettersep}

\VAR{ letter.salutation },

\begin{letterbody}
\BLOCK{ for paragraph in letter.paragraphs }

\VAR{ paragraph }
\BLOCK{ endfor }
\end{letterbody}

\vspace{\lettersep}
\VAR{ letter.closing },
\par\vspace{\lettersignskip}
\VAR{ profile.name }

\end{document}
